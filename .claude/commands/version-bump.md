---
description: "[claude-mem, reference only] Automated semantic versioning and release workflow for Claude Code plugins. Handles version increments across package.json, marketplace.json, plugin.json manifests, build verificat"
argument-hint: "[request]"
---

Use the `version-bump` skill (.claude/skills/version-bump/SKILL.md) to handle this request: $ARGUMENTS
