---
description: "Use for deep multi-page crawls, JS-rendered/dynamic sites, or structured web data extraction into LLM-ready Markdown/JSON via crawl4ai. Prefer WebFetch for a single quick page lookup instead."
argument-hint: "[request]"
---

Use the `crawl4ai` skill (.claude/skills/crawl4ai/SKILL.md) to handle this request: $ARGUMENTS
