---
description: "[claude-mem, reference only] Search claude-mem's persistent cross-session memory database. Use when user asks \"did we already solve this?\", \"how did we do X last time?\", or needs work from previous sessions."
argument-hint: "[request]"
---

Use the `mem-search` skill (.claude/skills/mem-search/SKILL.md) to handle this request: $ARGUMENTS
