---
description: "Search a codebase or UI for places that don't animate but should, and reject everything that shouldn't. Read-only; it proposes motion with exact values, it does not implement it. Use when the user ask"
argument-hint: "[request]"
---

Use the `find-animation-opportunities` skill (.claude/skills/find-animation-opportunities/SKILL.md) to handle this request: $ARGUMENTS
